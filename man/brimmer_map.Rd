% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/brimmer.R
\name{brimmer_map}
\alias{brimmer_map}
\title{Check many reported (mean, SD, n) rows against the SD bounds}
\usage{
brimmer_map(data, ..., include_inputs = TRUE)
}
\arguments{
\item{data}{A data frame, one reported statistic set per row.}

\item{...}{Constant arguments applied to all rows.}

\item{include_inputs}{If TRUE (default), returns \code{data} column-bound to the
results; if FALSE, only the result columns, in the same row order.}
}
\value{
A tibble of the \code{\link[=brimmer]{brimmer()}} columns, one row per input row
(optionally with the inputs prepended).
}
\description{
Applies \code{\link[=brimmer]{brimmer()}} to each row of a data frame, de-duplicating identical
constraint tuples so the rounding envelope is computed once per distinct
input and reused. Columns of \code{data} whose names match \code{\link[=brimmer]{brimmer()}} arguments
are taken per row; any argument given in \code{...} is a constant broadcast to
every row. Supplying one name both ways is an error.
}
\details{
Recognised names (column or constant): \code{l}, \code{u}, \code{a}, \code{b}, \code{n}, \code{mean},
\code{digits_mean}, \code{sd}, \code{digits_sd}, \code{rounding}, \code{granularity}, \code{scoring},
\code{n_items}, \code{alpha}.
}
\examples{
reports <- tibble::tibble(
  mean = c(2.97, 3.51, 4.20),
  sd = c(2.83, 3.50, 0.90),
  n = c(30, 30, 30)
)

# row 2 reports an SD above the ceiling for a 1-7 scale at n = 30
reports |>
  brimmer_map(l = 1, u = 7, digits_mean = 2, digits_sd = 2) |>
  dplyr::select("mean", "sd", "n", "consistent", "failed_tests")
}
